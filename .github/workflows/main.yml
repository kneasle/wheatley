name: "Lint"

on:
  pull_request:
    branches:
      - '**'
  push:
    branches:
      - master
  

jobs:
  linter:
    runs-on: ubuntu-latest

    steps:
    - name: Install dependencies
      run: |
        sudo apt-get install pylint3
    - name: Run linter
      run: pylint3 *.py
